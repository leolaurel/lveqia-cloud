group = 'com.lveqia.cloud.eureka'
version = '0.0.1-SNAPSHOT'


ext {
    springCloudVersion = 'Finchley.RC2'
}

dependencyManagement {
    imports {
        mavenBom "org.springframework.cloud:spring-cloud-dependencies:${springCloudVersion}"
    }
}
